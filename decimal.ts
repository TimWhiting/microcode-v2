namespace microcode {

    // we currently have 5 integer constants: 1,2,3,4,5 "dots"
    // let's preserve them in the UI, but change to decimal, and add 0
    // any decimal button can be edited to change the value, whereever it is
    
    // - a decimal field editor for both filters and modifiers
}